<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="https://fonts.googleapis.com/css?family=Do+Hyeon" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css">
  <link rel="stylesheet" href="/stylesheets/code_read.css">
  <title>친구 코드</title>
  <script>
      function password_check(){
       var code_passwordConfirm = document.getElementById('code_passwordConfirm').value;
       var code_password = '<%= rows[0].code_password %>';
        
       if(code_password != code_passwordConfirm){
         alert("패스워드가 틀렸습니다!");
       }
       else{
         return;
       }
      }
  </script>
</head>
<body>
    <div id="container">
      <div id=read_style>
        <input type="button" value="뒤로가기" class="backward_button" onclick="window.location.href='/friends_code'">
        <form method="POST" action="/code_delete" onsubmit="return password_check()">
    <table style="border: 3px solid black; width:100%"  id="example" class="display dataTable no-footer" role="grid" aria-describedby="example_info">
        <tr>
          <td>계정 아이디</td>
          <td><%= rows[0].ID %></td>
      </tr>
      <tr>
          <td>플랫폼</td>
          <td><%= rows[0].flatform %></td>
      </tr>
      <tr>
          <td>주 시간대</td>
          <td><%= rows[0].Main_time %></td>
      </tr>
      <tr>
          <td>지역</td>
          <td><%= rows[0].location %></td>
      </tr>
      <tr>
          <td>나이</td>
          <td><%= rows[0].age %></td>
      </tr>
      <tr>
          <td>한 줄 코멘트</td>
          <td><%= rows[0].comment %></td>
      </tr> 
      <tr>
        <td>비밀번호</td>
        <td>
            <input type="password" id="code_passwordConfirm" name="code_passwordConfirm" value="">
            <input type="text" id="confirm_id" name="confirm_id" hidden value="<%= rows[0].friends_ID %>">
        </td>
      </tr>
    
    </table>  
  <input type="submit" value="삭제하기" class="code_button">
</form>  
</div>

  </div>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
</body>
</html>